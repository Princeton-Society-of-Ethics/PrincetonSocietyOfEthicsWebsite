# Copy this file to `.env.local` for local overrides. All variables are optional.
#
# Base URL of the backend API, if/when one exists (no trailing slash).
# Leave unset to run the site fully static — forms will show a friendly
# "not available yet" message instead of calling a server.
# VITE_API_BASE_URL=https://api.example.com
